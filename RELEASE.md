# Bili.Dynamic.AutoDel v1.1.1

## v1.1.1

- 将油猴菜单“暂停 / 继续当前任务”更换为“终止脚本”，用于紧急终止当前脚本行为。

## v1.1.0

- 将删除任务完成通知的显示时长调整为 3 秒。
- 移除扫描开始时的重复 Windows 通知，保留扫描完成、停止和失败通知。
- 删除成功和暂停/继续不再弹出 Windows 通知。
- 删除进行中关闭进度面板只隐藏面板；暂停状态下关闭面板会终止任务并显示总结。

这是基于原作者 **monSteRhhe** 的
[bilibili-dynamic-del](https://github.com/monSteRhhe/bilibili-dynamic-del)
制作的修改版。

本版本的删除进度面板和任务总结弹窗，交互设计参考了
[ZhuTao3/bilibili-dynamic-del](https://github.com/ZhuTao3/bilibili-dynamic-del)，
并根据本项目现有流程重新实现。

## 主要变化

本版本基于 v1.0.0 发布，完善了候选动态筛选和预览排序，并增加预览列表顺序切换功能。

### 功能精简

- 移除“自动判断”。
- 移除“指定用户”。
- 移除“打开设置”。
- 移除取关功能及相关代码。

当前版本只保留：

- 选择指定日期之间的动态
- 终止脚本

### 新增功能

- 按开始日期和结束日期筛选用户转发动态。
- 预览候选动态按用户转发动态发布时间从新到旧稳定排序。
- 增加预览窗口“倒序”按钮，可切换新到旧和旧到新显示。
- 预览窗口标题改为“请选择需要删除的动态”，降低误删风险。
- 扫描过程显示可见进度窗口。
- 实时显示已扫描动态数量和候选数量。
- 扫描过程中可以停止扫描，并预览已扫描到的候选动态。
- 扫描成功、失败和停止时显示明确提示。
- 预览动态文字、图片、作者、UID、转发动态日期和动态链接。
- 支持全选、全不选和手动多选。
- 只有用户确认后才执行删除。
- 删除任务支持暂停和继续。
- API 请求及逐条删除之间增加时间间隔，减少操作频繁导致的失败。
- 删除阶段显示右下角进度面板，实时展示处理数量、成功数量、失败数量和暂停状态。
- 删除任务结束后显示居中的任务总结弹窗，失败明细默认收起并支持展开查看。
- 移除逐条删除成功和暂停/继续操作的 Windows 通知，避免遮挡右下角进度面板。
- 删除进行中关闭进度面板只隐藏面板；暂停状态下关闭面板会终止删除任务并显示总结。
- 移除扫描开始时的重复 Windows 通知，保留扫描完成、停止和失败通知。

### 问题修复

- 严格排除没有有效源动态 ID 的原创动态，只保留转发动态候选。
- 修复日期输入框重复弹出的问题。
- 修复日期为空时错误匹配全部历史动态的问题。
- 修复非法日期和日期范围校验问题。
- 修复删除请求中 CSRF Cookie 读取不正确的问题。
- 修复删除接口请求参数和错误信息显示问题。
- 增加删除请求失败时的控制台详细输出。
- 增加重复删除保护。
- 日期判断改为使用用户转发动态自身的发布时间，而不是源动态的发布时间。

## 使用注意

扫描结果只会进入预览列表，不会自动删除。请在确认日期范围和动态内容后，
手动勾选并确认删除。

动态删除属于实际账号操作，可能无法恢复。请勿在短时间内重复启动任务，并留意
Bilibili 接口变化和账号风控提示。

## 致谢与版权说明

本版本不是原创项目，基于 `monSteRhhe/bilibili-dynamic-del` 修改而来。
感谢原作者 monSteRhhe 提供基础实现。

本仓库包含 MIT License 文件。对于原项目代码，请继续保留原作者署名和原项目链接。

本版本目前仅在 Microsoft Edge 浏览器中通过测试，其他浏览器尚未测试，适配性未知。
